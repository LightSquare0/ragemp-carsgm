import GeneralInterface from "../General/GeneralInterface";

const GamemodeSelector: React.FC = (props) => {
  return (
    <>
      <GeneralInterface header="Select a gamemode">freeroam/race</GeneralInterface>
    </>
  );
};

export default GamemodeSelector;
